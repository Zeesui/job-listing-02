<% if flash.any? %>
  <% user_facing_flashes.each do |key, avlue| %>
  <div class="alert alert-dismissable alert-<%= flash_class(key) %>">
    <button class="close" data-dismiss="alert">x</bytton>
      <%= value %>
    </div>
    <% end %>
    <% end %>
